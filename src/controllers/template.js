import { Router } from 'express';

import handleSequelizeError from '../util/handleSequelizeError';

export default () => {
    const api = Router();

    api.post('/', (req, res) => {

    });

    api.get('/', (req, res) => {

    });

    api.put('/', (req, res) => {

    });

    api.delete('/', (req, res) => {

    });

    return api;
}

